extends layout

block content
	h1= title
	p Welcome to #{title}

	h2 Enter the URL:

	div.loginbox
		form(name="fetchURL", action="/scrape", method="post")
			input(type="text", name="url")
			input(type="submit", value="Fetch")